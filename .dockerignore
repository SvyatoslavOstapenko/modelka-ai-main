# Dependencies (created fresh via npm ci in Docker)
node_modules
.pnpm-store

# Build outputs (rebuilt in Docker)
.next
out
build
dist

# Development files
.git
.gitignore
*.md

# Local env files (sensitive!)
.env
.env.local
.env.*.local

# Docker stuff (avoid recursion)
docker-compose*.yml
Dockerfile
pg-data
pg-data-dev
acme.json

# IDE
.vscode
.idea

# Logs
npm-debug.log*
yarn-debug.log*

# Drizzle migrations are needed, don't ignore!
# drizzle/